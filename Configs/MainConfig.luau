return {
    StartLevel = "MainMenu",

    ObjectHoldDist = 9,
    ObjectMaxGrabDist = 9,

    MinimumPlayerPortalVelocity = 30,

    MakeTheModuleLoaderSHUTUP = nil, -- if value is nil, it will be automatically enabled or not depending on if the game is running in normally or in studio
    SafeLoad = false,
}